# Optional Vite defaults. Copy to .env.local only if you need a non-default
# gRPC URL. Login uses the Settings UI (account folder + password) — do not
# put passwords, private keys, or user.properties in env files.
VITE_ALTASTATA_GRPC_BASE_URL=http://127.0.0.1:9877
