# Copy to .env.local for local Vite defaults. Never commit real secrets.
VITE_ALTASTATA_GRPC_BASE_URL=http://127.0.0.1:9877
# Prefer choosing an account folder in the Settings UI (LoginV2).
# Do not put private keys or passwords in env files for shared machines.
